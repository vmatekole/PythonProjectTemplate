{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"","title":"FastVector","text":"<p>This is a simple vector python package.</p>"},{"location":"api/","title":"References","text":""},{"location":"api/#vector-class","title":"Vector Class","text":"<p>Own implementation of a 2D vector class.</p>"},{"location":"api/#fastvector.vector.Vector2D","title":"<code>Vector2D</code>","text":"<p>Vector2D class to perform simple vector operations.</p> Source code in <code>fastvector/vector.py</code> <pre><code>@total_ordering\nclass Vector2D:\n    \"\"\"Vector2D class to perform simple vector operations.\"\"\"\n\n    def __init__(self, x: float = 0.0, y: float = 0.0) -&gt; None:\n        \"\"\"Create a instance with the given x and y values.\n\n        Args:\n            x: x-Value.\n            y: y-Value.\n\n        Raises:\n            TypeError: If x or y are not a number.\n        \"\"\"\n        if isinstance(x, float | int) and isinstance(y, float | int):\n            self.x = x\n            self.y = y\n        else:\n            raise TypeError(\"You must pass in int/float values for x and y!\")\n\n    def __call__(self) -&gt; str:\n        \"\"\"Callable for the instance representation.\n\n        Returns:\n            The representation of the instance.\n        \"\"\"\n        print(\"Calling the __call__ function!\")\n        return self.__repr__()\n\n    def __repr__(self) -&gt; str:\n        \"\"\"Return the instance representation.\n\n        Returns:\n            The representation of the instance.\n        \"\"\"\n        return f\"vector.Vector2D({self.x}, {self.y})\"\n\n    def __str__(self) -&gt; str:\n        \"\"\"The instance as a string.\n\n        Returns:\n            The instance as a string.\n        \"\"\"\n        return f\"({self.x}, {self.y})\"\n\n    def __bool__(self) -&gt; bool:\n        \"\"\"Return the truth value of the instance.\n\n        Returns:\n            True, if the vector is not the Null-vector.\n            False, else.\n        \"\"\"\n        return bool(abs(self))\n\n    def __abs__(self) -&gt; float:\n        \"\"\"Return the length (magnitude) of the instance.\n\n        Returns:\n            Length of the instance.\n        \"\"\"\n        return sqrt(self.x**2.0 + self.y**2.0)\n\n    def __eq__(self, other_vector: object) -&gt; bool:\n        \"\"\"Check if the instances have the same values.\n\n        Args:\n            other_vector: Other instance (rhs of the operator)\n\n        Returns:\n            True, if the both instances have the same values.\n            False, else.\n        \"\"\"\n        if not isinstance(other_vector, Vector2D):\n            return False\n        return self.x == other_vector.x and self.y == other_vector.y\n\n    def __lt__(self, other_vector: Vector2D) -&gt; bool:\n        \"\"\"Check if the self instance is less than the other instance.\n\n        Args:\n            other_vector: Other instance (rhs of the operator).\n\n        Returns:\n            True, if the self instance is less than the other instance.\n            False, else.\n        \"\"\"\n        if not isinstance(other_vector, Vector2D):\n            raise TypeError(\"You must pass in a Vector2D instance!\")\n        return abs(self) &lt; abs(other_vector)\n\n    def __add__(self, other_vector: Vector2D) -&gt; Vector2D:\n        \"\"\"Returns the addition vector of the self and the other instance.\n\n        Args:\n            other_vector: Other instance (rhs of the operator).\n\n        Returns:\n            The addition vector of the self and the other instance.\n        \"\"\"\n        if not isinstance(other_vector, Vector2D):\n            raise TypeError(\"You must pass in a Vector2D instance!\")\n        x = self.x + other_vector.x\n        y = self.y + other_vector.y\n        return Vector2D(x, y)\n\n    def __sub__(self, other_vector: Vector2D) -&gt; Vector2D:\n        \"\"\"Return the subtraction vector of the self and the other instance.\n\n        Args:\n            other_vector: Other instance (rhs of the operator).\n\n        Returns:\n            The subtraction vector of the self and the other instance.\n        \"\"\"\n        if not isinstance(other_vector, Vector2D):\n            raise TypeError(\"You must pass in a Vector2D instance!\")\n        x = self.x - other_vector.x\n        y = self.y - other_vector.y\n        return Vector2D(x, y)\n\n    def __mul__(self, other: Vector2D | float) -&gt; float | Vector2D:\n        \"\"\"Return the multiplication of self and left vector or number.\n\n        Args:\n            other: Other instance or scaler value (rhs of the operator)\n\n        Raises:\n            TypeError: Not int/float passed in.\n\n        Returns:\n            The multiplication of self and left vector or number.\n        \"\"\"\n        if isinstance(other, Vector2D):\n            return self.x * other.x + self.y * other.y\n        if not isinstance(other, float):\n            raise TypeError(\"You must pass in an int/float!\")\n        return Vector2D(self.x * other, self.y * other)\n\n    def __truediv__(self, other: float) -&gt; Vector2D:\n        \"\"\"Return the multiplication of self and left vector or number.\n\n        Args:\n            other: Other instance or scaler value (rhs of the operator).\n\n        Raises:\n            ValueError: Division by zero.\n            TypeError: Not int/float passed in.\n\n        Returns:\n            The multiplication of self and left vector or number.\n        \"\"\"\n        if not isinstance(other, float):\n            raise TypeError(\"You must pass in an int/float!\")\n        return Vector2D(self.x / other, self.y / other)\n</code></pre>"},{"location":"api/#fastvector.vector.Vector2D.__abs__","title":"<code>__abs__()</code>","text":"<p>Return the length (magnitude) of the instance.</p> <p>Returns:</p> Type Description <code>float</code> <p>Length of the instance.</p> Source code in <code>fastvector/vector.py</code> <pre><code>def __abs__(self) -&gt; float:\n    \"\"\"Return the length (magnitude) of the instance.\n\n    Returns:\n        Length of the instance.\n    \"\"\"\n    return sqrt(self.x**2.0 + self.y**2.0)\n</code></pre>"},{"location":"api/#fastvector.vector.Vector2D.__add__","title":"<code>__add__(other_vector)</code>","text":"<p>Returns the addition vector of the self and the other instance.</p> <p>Parameters:</p> Name Type Description Default <code>other_vector</code> <code>Vector2D</code> <p>Other instance (rhs of the operator).</p> required <p>Returns:</p> Type Description <code>Vector2D</code> <p>The addition vector of the self and the other instance.</p> Source code in <code>fastvector/vector.py</code> <pre><code>def __add__(self, other_vector: Vector2D) -&gt; Vector2D:\n    \"\"\"Returns the addition vector of the self and the other instance.\n\n    Args:\n        other_vector: Other instance (rhs of the operator).\n\n    Returns:\n        The addition vector of the self and the other instance.\n    \"\"\"\n    if not isinstance(other_vector, Vector2D):\n        raise TypeError(\"You must pass in a Vector2D instance!\")\n    x = self.x + other_vector.x\n    y = self.y + other_vector.y\n    return Vector2D(x, y)\n</code></pre>"},{"location":"api/#fastvector.vector.Vector2D.__bool__","title":"<code>__bool__()</code>","text":"<p>Return the truth value of the instance.</p> <p>Returns:</p> Type Description <code>bool</code> <p>True, if the vector is not the Null-vector.</p> <code>bool</code> <p>False, else.</p> Source code in <code>fastvector/vector.py</code> <pre><code>def __bool__(self) -&gt; bool:\n    \"\"\"Return the truth value of the instance.\n\n    Returns:\n        True, if the vector is not the Null-vector.\n        False, else.\n    \"\"\"\n    return bool(abs(self))\n</code></pre>"},{"location":"api/#fastvector.vector.Vector2D.__call__","title":"<code>__call__()</code>","text":"<p>Callable for the instance representation.</p> <p>Returns:</p> Type Description <code>str</code> <p>The representation of the instance.</p> Source code in <code>fastvector/vector.py</code> <pre><code>def __call__(self) -&gt; str:\n    \"\"\"Callable for the instance representation.\n\n    Returns:\n        The representation of the instance.\n    \"\"\"\n    print(\"Calling the __call__ function!\")\n    return self.__repr__()\n</code></pre>"},{"location":"api/#fastvector.vector.Vector2D.__eq__","title":"<code>__eq__(other_vector)</code>","text":"<p>Check if the instances have the same values.</p> <p>Parameters:</p> Name Type Description Default <code>other_vector</code> <code>object</code> <p>Other instance (rhs of the operator)</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True, if the both instances have the same values.</p> <code>bool</code> <p>False, else.</p> Source code in <code>fastvector/vector.py</code> <pre><code>def __eq__(self, other_vector: object) -&gt; bool:\n    \"\"\"Check if the instances have the same values.\n\n    Args:\n        other_vector: Other instance (rhs of the operator)\n\n    Returns:\n        True, if the both instances have the same values.\n        False, else.\n    \"\"\"\n    if not isinstance(other_vector, Vector2D):\n        return False\n    return self.x == other_vector.x and self.y == other_vector.y\n</code></pre>"},{"location":"api/#fastvector.vector.Vector2D.__init__","title":"<code>__init__(x=0.0, y=0.0)</code>","text":"<p>Create a instance with the given x and y values.</p> <p>Parameters:</p> Name Type Description Default <code>x</code> <code>float</code> <p>x-Value.</p> <code>0.0</code> <code>y</code> <code>float</code> <p>y-Value.</p> <code>0.0</code> <p>Raises:</p> Type Description <code>TypeError</code> <p>If x or y are not a number.</p> Source code in <code>fastvector/vector.py</code> <pre><code>def __init__(self, x: float = 0.0, y: float = 0.0) -&gt; None:\n    \"\"\"Create a instance with the given x and y values.\n\n    Args:\n        x: x-Value.\n        y: y-Value.\n\n    Raises:\n        TypeError: If x or y are not a number.\n    \"\"\"\n    if isinstance(x, float | int) and isinstance(y, float | int):\n        self.x = x\n        self.y = y\n    else:\n        raise TypeError(\"You must pass in int/float values for x and y!\")\n</code></pre>"},{"location":"api/#fastvector.vector.Vector2D.__lt__","title":"<code>__lt__(other_vector)</code>","text":"<p>Check if the self instance is less than the other instance.</p> <p>Parameters:</p> Name Type Description Default <code>other_vector</code> <code>Vector2D</code> <p>Other instance (rhs of the operator).</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True, if the self instance is less than the other instance.</p> <code>bool</code> <p>False, else.</p> Source code in <code>fastvector/vector.py</code> <pre><code>def __lt__(self, other_vector: Vector2D) -&gt; bool:\n    \"\"\"Check if the self instance is less than the other instance.\n\n    Args:\n        other_vector: Other instance (rhs of the operator).\n\n    Returns:\n        True, if the self instance is less than the other instance.\n        False, else.\n    \"\"\"\n    if not isinstance(other_vector, Vector2D):\n        raise TypeError(\"You must pass in a Vector2D instance!\")\n    return abs(self) &lt; abs(other_vector)\n</code></pre>"},{"location":"api/#fastvector.vector.Vector2D.__mul__","title":"<code>__mul__(other)</code>","text":"<p>Return the multiplication of self and left vector or number.</p> <p>Parameters:</p> Name Type Description Default <code>other</code> <code>Vector2D | float</code> <p>Other instance or scaler value (rhs of the operator)</p> required <p>Raises:</p> Type Description <code>TypeError</code> <p>Not int/float passed in.</p> <p>Returns:</p> Type Description <code>float | Vector2D</code> <p>The multiplication of self and left vector or number.</p> Source code in <code>fastvector/vector.py</code> <pre><code>def __mul__(self, other: Vector2D | float) -&gt; float | Vector2D:\n    \"\"\"Return the multiplication of self and left vector or number.\n\n    Args:\n        other: Other instance or scaler value (rhs of the operator)\n\n    Raises:\n        TypeError: Not int/float passed in.\n\n    Returns:\n        The multiplication of self and left vector or number.\n    \"\"\"\n    if isinstance(other, Vector2D):\n        return self.x * other.x + self.y * other.y\n    if not isinstance(other, float):\n        raise TypeError(\"You must pass in an int/float!\")\n    return Vector2D(self.x * other, self.y * other)\n</code></pre>"},{"location":"api/#fastvector.vector.Vector2D.__repr__","title":"<code>__repr__()</code>","text":"<p>Return the instance representation.</p> <p>Returns:</p> Type Description <code>str</code> <p>The representation of the instance.</p> Source code in <code>fastvector/vector.py</code> <pre><code>def __repr__(self) -&gt; str:\n    \"\"\"Return the instance representation.\n\n    Returns:\n        The representation of the instance.\n    \"\"\"\n    return f\"vector.Vector2D({self.x}, {self.y})\"\n</code></pre>"},{"location":"api/#fastvector.vector.Vector2D.__str__","title":"<code>__str__()</code>","text":"<p>The instance as a string.</p> <p>Returns:</p> Type Description <code>str</code> <p>The instance as a string.</p> Source code in <code>fastvector/vector.py</code> <pre><code>def __str__(self) -&gt; str:\n    \"\"\"The instance as a string.\n\n    Returns:\n        The instance as a string.\n    \"\"\"\n    return f\"({self.x}, {self.y})\"\n</code></pre>"},{"location":"api/#fastvector.vector.Vector2D.__sub__","title":"<code>__sub__(other_vector)</code>","text":"<p>Return the subtraction vector of the self and the other instance.</p> <p>Parameters:</p> Name Type Description Default <code>other_vector</code> <code>Vector2D</code> <p>Other instance (rhs of the operator).</p> required <p>Returns:</p> Type Description <code>Vector2D</code> <p>The subtraction vector of the self and the other instance.</p> Source code in <code>fastvector/vector.py</code> <pre><code>def __sub__(self, other_vector: Vector2D) -&gt; Vector2D:\n    \"\"\"Return the subtraction vector of the self and the other instance.\n\n    Args:\n        other_vector: Other instance (rhs of the operator).\n\n    Returns:\n        The subtraction vector of the self and the other instance.\n    \"\"\"\n    if not isinstance(other_vector, Vector2D):\n        raise TypeError(\"You must pass in a Vector2D instance!\")\n    x = self.x - other_vector.x\n    y = self.y - other_vector.y\n    return Vector2D(x, y)\n</code></pre>"},{"location":"api/#fastvector.vector.Vector2D.__truediv__","title":"<code>__truediv__(other)</code>","text":"<p>Return the multiplication of self and left vector or number.</p> <p>Parameters:</p> Name Type Description Default <code>other</code> <code>float</code> <p>Other instance or scaler value (rhs of the operator).</p> required <p>Raises:</p> Type Description <code>ValueError</code> <p>Division by zero.</p> <code>TypeError</code> <p>Not int/float passed in.</p> <p>Returns:</p> Type Description <code>Vector2D</code> <p>The multiplication of self and left vector or number.</p> Source code in <code>fastvector/vector.py</code> <pre><code>def __truediv__(self, other: float) -&gt; Vector2D:\n    \"\"\"Return the multiplication of self and left vector or number.\n\n    Args:\n        other: Other instance or scaler value (rhs of the operator).\n\n    Raises:\n        ValueError: Division by zero.\n        TypeError: Not int/float passed in.\n\n    Returns:\n        The multiplication of self and left vector or number.\n    \"\"\"\n    if not isinstance(other, float):\n        raise TypeError(\"You must pass in an int/float!\")\n    return Vector2D(self.x / other, self.y / other)\n</code></pre>"}]}